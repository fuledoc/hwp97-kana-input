# HWP97 Kana Input 개발 규칙

## 범위

- 이 저장소는 한글 발음을 일본어 가나로 바꾸는 정적 웹앱이다. 실사용자는 비개발자(일본어 전공 은퇴 교수)이며, 일본어 책 제목을 중고책 플랫폼에 등록할 때 쓴다. 어느 컴퓨터에서든 접속하는 단순·심플한 사용성이 최우선이다.
- 한글97 바이너리나 사전 파일을 추출·복사·수정하지 않는다.
- `archive/`는 이전 배포본 보관용이다. 개발 중 수정하거나 압축을 풀지 않는다.
- 변환은 번역이 아니라 발음 기반 휴리스틱이다. Hangul Phonetic 계약은 `test/fixtures/hangul-phonetic.golden.json`, 근거와 한계는 `docs/hangul-phonetic-policy.md`에 둔다.

## 구조 불변 조건

- `src/kana-engine.js`는 DOM에 의존하지 않는 순수 변환 엔진으로 유지한다.
- `src/app.js`는 브라우저 입력 상태, 선택 영역, IME 이벤트와 UI 연결만 담당한다.
- 엔진의 CommonJS/browser UMD 공개 API와 `{ text, rest }` 반환 형태는 호환성 검토 없이 바꾸지 않는다.
- 런타임 의존성이나 빌드 단계를 추가하기 전에 이 정적 앱에 필요한 이유를 설명한다.

## UI와 사용성 불변 조건

- 화면에는 Hangul 발음 입력만 노출한다. Romaji/JIS 엔진은 삭제하지 말고 UI에서만 숨긴다. `currentScheme()`은 `scheme` 라디오가 있을 때만 그 값을 따르고 없으면 `hangul`로 고정한다(테스트 하네스는 라디오를 주입해 Romaji/JIS 경로를 계속 검증한다).
- 사용자 대면 문구는 한국어로 유지한다. `HWP97`·`Editor`·`buffer` 같은 개발자 용어를 화면에 노출하지 않는다. 히라가나/카타카나 표기는 일본어 전공자 대상이라 그대로 둔다.
- 「복사하기」는 화면에서 가장 뚜렷한 주 버튼으로, 「지우기」는 시각적으로 약하게·비어 있지 않으면 확인을 거쳐 데이터 손실을 막는다.
- 한글로 치기 어려운 글자(`を`, `ー`, 「」, `・`)는 「특수 글자」 빠른삽입 버튼으로 제공한다. 삽입 전 대기 중인 Hangul 변환을 먼저 확정한다.

## 한자 변환 불변 조건

- **한자를 자동으로 삽입하지 않는다.** 반드시 사용자가 후보 버튼을 눌러야 바뀐다. 책 제목은 틀리면 판매가 안 되므로 눈으로 확인하고 승인하는 단계를 없애지 않는다.
- 조합(`compositionstart`~`compositionend`) 중, `historyUndo`/`historyRedo` 직후, 카타카나 모드에서는 한자 패널을 띄우지 않는다.
- 사전 로딩이 실패하면 한자 기능만 조용히 꺼지고 가나 변환·복사하기는 언제나 동작한다. 「사전을 불러오지 못했습니다」류 메시지와 로딩 스피너를 넣지 않는다.
- 사전에 걸리는 단어가 없으면 아무것도 렌더하지 않는다. 실패 메시지 대신 침묵한다.
- 한자 후보 버튼에 채움색을 쓰지 않는다. 화면의 주 버튼은 「복사하기」 하나다.
- 후보 목록의 첫 항목은 언제나 원문 가나다. 고르기와 되돌리기가 같은 동작이어야 한다.
- 사전(`dict/kanji.txt`)은 SKK-JISYO.L 가공본이며 GPL-2.0+이다. 헤더의 저작권·라이선스 고지와 `dict/COPYING`을 지운 채 배포하지 않는다. CC BY-SA 사전을 같은 파일에 합치지 않는다.

## 책 목록·표지 불변 조건

- 표지 이미지는 저장소 안(`photos/covers/{ISBN13}.jpg`)에 두고 상대경로로 참조한다. 서점 이미지를 **핫링크하지 않는다**(외부 요청 0건 조건과 직결).
- 표지를 추가·교체할 때 이미지를 실제로 열어 표지에 인쇄된 제목·저자·출판사·총서번호를 목록과 대조한다. 서점 페이지의 설명문이나 검색 스니펫만으로 확정하지 않는다. 판(단행본/신서/문고)·권(上下·1·2)이 다르면 표지가 전혀 다르다.
- Amazon은 표지가 없을 때 「COVER COMING SOON」 자리표시자를 정상 크기 JPEG로 돌려준다. 파일 크기로 거를 수 없으므로 눈으로 확인하거나 알려진 자리표시자와 지각 해시로 비교한다.
- 서지(발행년·쪽수)는 국립국회도서관 서치의 ISBN 조회 결과를 기준으로 삼는다. 목록 값과 어긋나면 어느 판의 값인지 밝히기 전까지 고치지 않는다.
- 판이 갈려 실물 판권장(奥付) 확인이 필요한 항목은 화면에 「판 확인 필요」로 표시하고 근거를 「이 목록은 어떻게 만들었나요?」에 남긴다.
- `books.html`은 판매 실무용 자료라 `robots: noindex`를 유지한다.
- 항목의 순서와 이름은 알라딘 상품 등록 화면(`scm/wrecord.aspx`)을 따른다: ISBN · 도서명 · 부제 · 저자 · 출판사 · 페이지 · 출간일. 임의로 재배열하지 않는다.
- 「출판사」에 총서명(`集英社新書 0509A`)을 넣지 않는다. 알라딘 출판사 칸에는 발행처(`集英社`)가 들어가야 한다. 총서는 별도 「총서」 항목으로 둔다.
- 분류는 알라딘 상품페이지의 `ul#ulCategory` 값을 그대로 쓰고, 알라딘에 없는 책만 제안값을 넣되 화면에 「제안값」으로 표시한다. 알라딘이 쓰지 않는 분류명을 지어내지 않는다.
- 한 행의 모든 값은 그 행의 ISBN 한 판(printing)에 대응해야 한다. 쪽수는 A판, 발행년은 B판처럼 섞지 않는다.
- 목록의 각 항목(제목·저자·출판사·발행년·쪽수·ISBN)은 누르면 그 값만 복사된다. 알라딘 등록 화면의 칸에 하나씩 붙여넣는 용도이므로, 쪽수는 「167쪽」으로 보여 주되 복사값은 숫자 `167`처럼 **붙여넣을 값 그대로** 둔다.
- 표지는 `<a download>`로 감싸 눌러서 저장되게 한다. `download`는 동일 출처에서만 동작하므로 표지를 외부에서 핫링크하면 이 기능이 조용히 깨진다.
- 알라딘 검색 여부(상태)는 표지 **위**, 「표지 저장」(동작)은 표지 **아래** 둔다. 둘을 아래에 나란히 두면 한 덩어리로 보여 그냥 지나친다. 상태 표시는 링크 안에 넣지 않는다.
- 색으로 역할을 가른다: 초록은 상태, 브랜드 청록은 누르는 것(「표지 저장」·「제목 복사」). 상태 표시에 브랜드색을 쓰지 않는다.
- 상태 표시가 표지 그림을 덮지 않게 한다. 실물 책과 대조할 그림이라 제목이 가려지면 안 된다.
- 표지 이미지는 가로 500px을 넘기지 않는다(알라딘 상품 이미지 최대 가로 500px).
- `books.html`이 참조하는 `books.js`/`books.css`/`styles.css`의 동작을 바꾸면 `?v=` 번호를 올린다. 서비스워커가 없어 캐시를 지울 수단이 없고, 비개발자에게 「눌러도 아무 일이 없음」으로 나타난다.

## 배포 불변 조건

- 정적 호스팅(GitHub Pages)만 사용한다. 스토어 제출·서명·버전코드는 이 앱에 해당 없다.
- 외부 요청·수집·애널리틱스·외부 CDN/폰트를 추가하지 않는다. 릴리스 전 `grep -rEn 'https?://' index.html books.html src/`가 비어 있는지 확인한다.
- 자원은 상대경로만 쓴다(서브패스 호스팅 호환). 절대경로/`<base href>`를 도입하지 않는다.
- 서비스워커·오프라인 캐시는 넣지 않는다(비개발자에게 스테일 캐시 혼란을 유발). 필요해지면 network-first 최소 SW를 별도 검토한다.

## IME 안전 조건

- `compositionstart`부터 `compositionend`까지 활성 조합 문자열을 변환하지 않는다.
- `compositionend` 뒤 마지막 `input`을 기다리기 위한 120ms fallback을 유지한다.
- 새 조합은 낡은 타이머를 취소하되 이전 확정 범위를 폐기하지 않는다.
- 변환은 문서 전체가 아니라 확정된 입력 범위에만 적용한다.
- `historyUndo`와 `historyRedo` 결과를 자동 재변환하지 않는다.
- 선택 방향, 커서, 스크롤을 보존한다.
- Romaji/JIS `keydown`은 `isComposing`, `Process`, `keyCode 229` 상태에서 가로채지 않는다.
- IME 처리 변경은 자동 테스트와 Windows 11 Edge/Chrome 수동 테스트를 모두 거친다.

## 검증

```sh
npm test
npm run check
npm run verify
```

- Hangul Phonetic 매핑을 바꿀 때 구현, 골든 코퍼스, 변환 정책을 함께 갱신한다. Romaji/JIS 사례는 `test/kana-engine.test.js` 또는 해당 경계 테스트에 추가한다.
- 외부 IME 규칙을 채택할 때는 가변 브랜치 전체를 암묵적으로 추종하지 않고, 출처의 commit과 실제 채택한 규칙을 기록한다.
- 골든 기대값은 리터럴로 사람이 검토한다. 현재 엔진 출력으로 생성하거나 실패를 없애기 위해 일괄 덮어쓰지 않는다.
- `provisional` 사례도 회귀 계약이다. 의도적으로 정책을 결정하기 전에는 기대값을 바꾸지 않는다.
- 전체 11,172개 한글 음절의 한글 잔존 없음·Katakana 대칭 불변식 테스트를 유지한다.
- 이벤트 처리를 바꿀 때 조합·타이머·모드 전환 회귀를 `test/app.test.js`에 추가한다.
- Windows 릴리스 전에는 `docs/windows-ime-test.md`를 수행한다.

## 에이전트 운용

- 작은 변경은 한 에이전트가 구현과 테스트를 함께 맡는다.
- 독립 검토가 유리한 큰 변경만 `엔진/골든 테스트`, `UI/IME 상태`, `문서/QA`로 병렬화한다.
- 병렬 에이전트는 같은 파일을 동시에 편집하지 않는다. 주 에이전트가 공개 API와 최종 통합을 책임진다.
- 인계에는 변경 파일, 실행한 검사, Windows에서 남은 수동 확인을 명시한다.
- 별도 커스텀 스킬은 여러 저장소에서 가나 코퍼스 또는 Windows IME 검증 절차를 반복하게 될 때 만든다.
